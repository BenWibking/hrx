# Copyright 2026 The IREE Authors
#
# Licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

load("//build_tools/testing:build_defs.bzl", "iree_execution_test_suite")
load(
    "//loom/build_tools/bazel:build_defs.bzl",
    "loom_cc_binary",
    "loom_cc_library",
    "loom_cc_test",
)

package(
    default_visibility = ["//visibility:private"],
    features = ["layering_check"],
    licenses = ["notice"],  # Apache 2.0
)

loom_cc_library(
    name = "diagnostic_report",
    srcs = ["diagnostic_report.c"],
    hdrs = ["diagnostic_report.h"],
    deps = [
        "//loom/src/loom/error:error_defs",
        "//runtime/src/iree/base",
        "//runtime/src/iree/base/internal:json",
    ],
)

loom_cc_binary(
    name = "loom-corpus-compile-xfail",
    srcs = ["compile_xfail.c"],
    visibility = ["//visibility:public"],
    deps = [
        ":diagnostic_report",
        "//loom/src/loom/target/tool:process",
        "//runtime/src/iree/base",
        "//runtime/src/iree/base/tooling:flags",
        "//runtime/src/iree/io:file_handle",
    ],
)

loom_cc_test(
    name = "diagnostic_report_test",
    srcs = ["diagnostic_report_test.cc"],
    deps = [
        ":diagnostic_report",
        "//runtime/src/iree/base",
        "//runtime/src/iree/testing:gtest",
        "//runtime/src/iree/testing:gtest_main",
    ],
)

iree_execution_test_suite(
    name = "compile_xfail_execution_test",
    data = [
        "testdata/diagnostic_xfail.loom",
    ],
    manifests = ["compile_xfail.test.json"],
    tags = ["hostonly"],
    tools = {
        "loom-compile": "//loom/src/loom/tools/loom-compile",
        "loom-corpus-compile-xfail": ":loom-corpus-compile-xfail",
    },
)
