check.case @clean {
  check.return
}

check.benchmark<@clean> @clean_benchmark

check.case @missing_event {
  check.expect.event<device> {count = 1, type = "asan_report"}
  check.return
}

check.benchmark<@missing_event> @missing_event_benchmark
