// RUN: emit source-low output=low

amdgpu.target<gfx11-generic> @target

func.def target(@target) @uniform_refined_offset_word(%address: offset) -> (i64, i64) {
  %bounded = index.assume %address [range(%address, 0, 4294967295)] : offset
  %word = index.cast %bounded : offset to i32
  %signed = scalar.extsi %word : i32 to i64
  %unsigned = scalar.extui %word : i32 to i64
  func.return %signed, %unsigned : i64, i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @uniform_refined_offset_word(%bounded: reg<amdgpu.sgpr x2>) -> (reg<amdgpu.sgpr x2>, reg<amdgpu.sgpr x2>) asm {
  %word = slice %bounded[0] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  %11 = s_ashr_i32_rhs_inline %word, 31
  %signed = concat(%word, %11) : (reg<amdgpu.sgpr>, reg<amdgpu.sgpr>) -> reg<amdgpu.sgpr x2>
  %13 = s_mov_b32 0
  %unsigned = concat(%word, %13) : (reg<amdgpu.sgpr>, reg<amdgpu.sgpr>) -> reg<amdgpu.sgpr x2>
  return %signed, %unsigned
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @uniform_refined_offset_wide(%address: offset) -> (i64) {
  %bounded = index.assume %address [range(%address, 0, 4294967295)] : offset
  %result = index.cast %bounded : offset to i64
  func.return %result : i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @uniform_refined_offset_wide(%bounded: reg<amdgpu.sgpr x2>) -> (reg<amdgpu.sgpr x2>) asm {
  %6 = slice %bounded[0] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  %7 = s_mov_b32 0
  %result = concat(%6, %7) : (reg<amdgpu.sgpr>, reg<amdgpu.sgpr>) -> reg<amdgpu.sgpr x2>
  return %result
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @varying_refined_offset_word(%values: vector<2xi64>) -> (i64, i64) {
  %raw = vector.extract %values[0] : vector<2xi64> -> i64
  %nonnegative = scalar.assume %raw [range(%raw, 0, 9223372036854775807)] : i64
  %address = index.cast %nonnegative : i64 to offset
  %bounded = index.assume %address [range(%address, 0, 4294967295)] : offset
  %word = index.cast %bounded : offset to i32
  %signed = scalar.extsi %word : i32 to i64
  %unsigned = scalar.extui %word : i32 to i64
  func.return %signed, %unsigned : i64, i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @varying_refined_offset_word(%values: reg<amdgpu.vgpr x4>) -> (reg<amdgpu.vgpr x2>, reg<amdgpu.vgpr x2>) asm {
  %word = slice %values[0] : reg<amdgpu.vgpr x4> -> reg<amdgpu.vgpr>
  %15 = v_ashrrev_i32_src0_inline %word, 31
  %signed = concat(%word, %15) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x2>
  %17 = v_mov_b32 0
  %unsigned = concat(%word, %17) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x2>
  return %signed, %unsigned
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @varying_refined_offset_wide(%values: vector<2xi64>) -> (i64) {
  %raw = vector.extract %values[0] : vector<2xi64> -> i64
  %nonnegative = scalar.assume %raw [range(%raw, 0, 9223372036854775807)] : i64
  %address = index.cast %nonnegative : i64 to offset
  %bounded = index.assume %address [range(%address, 0, 4294967295)] : offset
  %result = index.cast %bounded : offset to i64
  func.return %result : i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @varying_refined_offset_wide(%values: reg<amdgpu.vgpr x4>) -> (reg<amdgpu.vgpr x2>) asm {
  %13 = slice %values[0] : reg<amdgpu.vgpr x4> -> reg<amdgpu.vgpr>
  %11 = v_mov_b32 0
  %result = concat(%13, %11) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x2>
  return %result
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @wide_offset_i32(%address: offset) -> (i32) {
  %word = index.cast %address : offset to i32
  func.return %word : i32
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @wide_offset_i32(%address: reg<amdgpu.sgpr x2>) -> (reg<amdgpu.sgpr>) asm {
  %word = slice %address[0] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %word
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @nonnegative_index_i64(%address: index) -> (i64) {
  %bounded = index.assume %address [range(%address, 0, 9223372036854775807)] : index
  %word = index.cast %bounded : index to i64
  func.return %word : i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @nonnegative_index_i64(%bounded: reg<amdgpu.sgpr>) -> (reg<amdgpu.sgpr x2>) asm {
  %6 = s_mov_b32 0
  %word = concat(%bounded, %6) : (reg<amdgpu.sgpr>, reg<amdgpu.sgpr>) -> reg<amdgpu.sgpr x2>
  return %word
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @wide_index_i32(%value: i64) -> (i64) {
  %nonnegative = scalar.assume %value [range(%value, 0, 9223372036854775807)] : i64
  %address = index.cast %nonnegative : i64 to index
  %bounded = index.assume %address [range(%address, 0, 2147483647)] : index
  %word = index.cast %bounded : index to i32
  %result = scalar.extsi %word : i32 to i64
  func.return %result : i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @wide_index_i32(%bounded: reg<amdgpu.sgpr x2>) -> (reg<amdgpu.sgpr x2>) asm {
  %word = slice %bounded[0] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  %10 = s_ashr_i32_rhs_inline %word, 31
  %result = concat(%word, %10) : (reg<amdgpu.sgpr>, reg<amdgpu.sgpr>) -> reg<amdgpu.sgpr x2>
  return %result
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @varying_nonnegative_index_i64(%values: vector<2xi32>) -> (i64) {
  %value = vector.extract %values[0] : vector<2xi32> -> i32
  %address = index.cast %value : i32 to index
  %bounded = index.assume %address [range(%address, 0, 9223372036854775807)] : index
  %result = index.cast %bounded : index to i64
  func.return %result : i64
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @varying_nonnegative_index_i64(%values: reg<amdgpu.vgpr x2>) -> (reg<amdgpu.vgpr x2>) asm {
  %bounded = slice %values[0] : reg<amdgpu.vgpr x2> -> reg<amdgpu.vgpr>
  %9 = v_mov_b32 0
  %result = concat(%bounded, %9) : (reg<amdgpu.vgpr>, reg<amdgpu.vgpr>) -> reg<amdgpu.vgpr x2>
  return %result
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @varying_wide_address_i32(%values: vector<2xi64>) -> (i32) {
  %value = vector.extract %values[0] : vector<2xi64> -> i64
  %address_input = scalar.assume %value [range(%value, 0, 9223372036854775807)] : i64
  %address = index.cast %address_input : i64 to offset
  %result = index.cast %address : offset to i32
  func.return %result : i32
}

// ----
low.func.def target<amdgpu.gfx11.generic.core>(@target) abi(hal_kernel) @varying_wide_address_i32(%values: reg<amdgpu.vgpr x4>) -> (reg<amdgpu.vgpr>) asm {
  %result = slice %values[0] : reg<amdgpu.vgpr x4> -> reg<amdgpu.vgpr>
  return %result
}
