################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/wasm/BUILD.bazel                                   #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_generated_cc_library(
  NAME
    error_defs
  GENERATOR
    loom::py::loom::gen::error::c_errors
  SOURCE
    "error_catalog.c"
  GENERATED_HDR_FLAGS
    "--header"
  GENERATED_HDRS
    "error_catalog.h"
  ARGS
    "--catalog-symbol=loom_wasm_error_catalog"
    "--fallback-catalog-symbol=loom_error_catalog_core"
    "--fallback-public-header=loom/error/error_catalog.h"
    "--public-header=loom/target/emit/wasm/error_catalog.h"
    "--shard=wasm"
  DEPS
    loom::error::error_defs
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    binary_writer
  HDRS
    "binary_writer.h"
  SRCS
    "binary_writer.c"
  DEPS
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    types
  HDRS
    "types.h"
  SRCS
    "types.c"
  DEPS
    iree::base
    loom::target::arch::wasm::descriptors::descriptors_ids
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    program
  HDRS
    "program.h"
  DEPS
    ::types
    iree::base
    loom::ir
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    function_body
  HDRS
    "function_body.h"
  SRCS
    "function_body.c"
  DEPS
    ::binary_writer
    ::program
    ::types
    iree::base
    loom::codegen::low::descriptors
    loom::codegen::low::target_binding
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::target::arch::wasm::descriptors
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    module_binary
  HDRS
    "module_binary.h"
  SRCS
    "module_binary.c"
  DEPS
    ::binary_writer
    ::function_body
    ::program
    ::types
    iree::base
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_test(
  NAME
    descriptor_fields_test
  SRCS
    "descriptor_fields_test.cc"
  DEPS
    iree::testing::gtest
    iree::testing::gtest_main
    loom::codegen::low::descriptors
    loom::ir
    loom::target::arch::wasm::descriptors::descriptors_ids
  LABELS
    "iree-build-requirement=loom.target.arch.wasm"
    "iree-build-requirement=loom.emit.wasm"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
