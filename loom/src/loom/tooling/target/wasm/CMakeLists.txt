################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/wasm/BUILD.bazel                                #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    prepare
  HDRS
    "prepare.h"
  SRCS
    "prepare.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::codegen::low::allocation
    loom::codegen::low::descriptors
    loom::codegen::low::function_model
    loom::error::emitter
    loom::ir
    loom::ops::low
    loom::ops::op_defs
    loom::target::arch::wasm::descriptors
    loom::target::emit::wasm::module_binary
    loom::target::emit::wasm::program
    loom::target::registers
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    artifact_emitter
  HDRS
    "artifact_emitter.h"
  SRCS
    "artifact_emitter.c"
  DEPS
    ::prepare
    iree::base
    loom::target::arch::wasm::ops
    loom::target::provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    loom_check
  HDRS
    "check/loom_check.h"
  SRCS
    "check/loom_check.c"
  DEPS
    ::prepare
    iree::base
    loom::target::tool::wasm
    loom::tools::loom-check::execute
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_WASM AND LOOM_EMIT_WASM)
loom_cc_library(
  NAME
    check_provider
  HDRS
    "check/provider.h"
  SRCS
    "check/provider.c"
  DEPS
    ::loom_check
    iree::base
    loom::tools::loom-check::provider
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
