diff --git a/packages/cxx-gen-ast/src/gen_semantic_codec.ts b/packages/cxx-gen-ast/src/gen_semantic_codec.ts
--- a/packages/cxx-gen-ast/src/gen_semantic_codec.ts
+++ b/packages/cxx-gen-ast/src/gen_semantic_codec.ts
@@ -47,7 +47,8 @@
   { tag: 7, cpp: "cxx::ConstObject", kind: "shared" },
   { tag: 8, cpp: "cxx::ConstAddress", kind: "shared" },
   { tag: 9, cpp: "cxx::ConstLabelAddress", kind: "shared" },
-  { tag: 10, cpp: "cxx::IndeterminateValue", kind: "indeterminate" },
+  { tag: 10, cpp: "cxx::ConstComplex", kind: "shared" },
+  { tag: 11, cpp: "cxx::IndeterminateValue", kind: "indeterminate" },
 ] as const;

 const sharedConstKinds = constAlternatives
@@ -278,6 +279,13 @@
   const out: string[] = [];
   const emit = (line = "") => out.push(line);

+  emit(`static_assert(std::variant_size_v<ConstValue> == ${constAlternatives.length});`);
+  for (const alternative of constAlternatives) {
+    const type = alternative.kind === "shared"
+      ? `std::shared_ptr<${alternative.cpp}>` : alternative.cpp;
+    emit(`static_assert(std::is_same_v<std::variant_alternative_t<${alternative.tag}, ConstValue>, ${type}>);`);
+  }
+  emit();
   emit(encoderEntryPoint(plan));
   emit(encoderDispatch(plan));
   emit(encoderHelpers());
diff --git a/src/parser/cxx/private/semantic_codec.h b/src/parser/cxx/private/semantic_codec.h
--- a/src/parser/cxx/private/semantic_codec.h
+++ b/src/parser/cxx/private/semantic_codec.h
@@ -92,6 +92,11 @@
   [[nodiscard]] auto constRef(
       const std::shared_ptr<cxx::ConstLabelAddress>& value) -> ConstRef {
     return ConstRef{constNodeRef(value, 4)};
+  }
+
+  [[nodiscard]] auto constRef(const std::shared_ptr<cxx::ConstComplex>& value)
+      -> ConstRef {
+    return ConstRef{constNodeRef(value, 5)};
   }

   template <typename T>
diff --git a/src/parser/cxx/semantic_codec.cc b/src/parser/cxx/semantic_codec.cc
--- a/src/parser/cxx/semantic_codec.cc
+++ b/src/parser/cxx/semantic_codec.cc
@@ -50,6 +50,31 @@

 }  // namespace

+static_assert(std::variant_size_v<ConstValue> == 12);
+static_assert(
+    std::is_same_v<std::variant_alternative_t<0, ConstValue>, std::intmax_t>);
+static_assert(std::is_same_v<std::variant_alternative_t<1, ConstValue>,
+                             const cxx::StringLiteral*>);
+static_assert(std::is_same_v<std::variant_alternative_t<2, ConstValue>, float>);
+static_assert(
+    std::is_same_v<std::variant_alternative_t<3, ConstValue>, double>);
+static_assert(
+    std::is_same_v<std::variant_alternative_t<4, ConstValue>, long double>);
+static_assert(std::is_same_v<std::variant_alternative_t<5, ConstValue>,
+                             std::shared_ptr<cxx::Meta>>);
+static_assert(std::is_same_v<std::variant_alternative_t<6, ConstValue>,
+                             std::shared_ptr<cxx::InitializerList>>);
+static_assert(std::is_same_v<std::variant_alternative_t<7, ConstValue>,
+                             std::shared_ptr<cxx::ConstObject>>);
+static_assert(std::is_same_v<std::variant_alternative_t<8, ConstValue>,
+                             std::shared_ptr<cxx::ConstAddress>>);
+static_assert(std::is_same_v<std::variant_alternative_t<9, ConstValue>,
+                             std::shared_ptr<cxx::ConstLabelAddress>>);
+static_assert(std::is_same_v<std::variant_alternative_t<10, ConstValue>,
+                             std::shared_ptr<cxx::ConstComplex>>);
+static_assert(std::is_same_v<std::variant_alternative_t<11, ConstValue>,
+                             cxx::IndeterminateValue>);
+
 auto SemanticEncoder::operator()(const SemanticArchiveRoots& roots,
                                  ArchiveWriter& archive) -> bool {
   ByteWriter session;
@@ -1417,6 +1442,10 @@
       writecxxConstLabelAddress(
           out, static_cast<const cxx::ConstLabelAddress*>(node.owner.get()));
       break;
+    case 5:
+      writecxxConstComplex(
+          out, static_cast<const cxx::ConstComplex*>(node.owner.get()));
+      break;
     default:
       reportError("unknown constant node kind");
       break;
@@ -1539,6 +1568,10 @@
       break;
     }
     case 10: {
+      out.varU32(static_cast<std::uint32_t>(constRef(std::get<10>(value))));
+      break;
+    }
+    case 11: {
       break;
     }
   }
@@ -9354,6 +9387,9 @@
       return cxx::ConstValue{std::static_pointer_cast<cxx::ConstLabelAddress>(
           constantAt(ConstRef{in.varU32()}))};
     case 10:
+      return cxx::ConstValue{std::static_pointer_cast<cxx::ConstComplex>(
+          constantAt(ConstRef{in.varU32()}))};
+    case 11:
       return cxx::ConstValue{cxx::IndeterminateValue{}};
     default:
       fail("unknown constant value alternative");
@@ -9419,6 +9455,12 @@
       auto value = std::make_shared<cxx::ConstLabelAddress>();
       constants_[index - 1] = value;
       readcxxConstLabelAddress(in, value.get());
+      return value;
+    }
+    case 5: {
+      auto value = std::make_shared<cxx::ConstComplex>();
+      constants_[index - 1] = value;
+      readcxxConstComplex(in, value.get());
       return value;
     }
     default:
diff --git a/src/parser/cxx/archive.h b/src/parser/cxx/archive.h
--- a/src/parser/cxx/archive.h
+++ b/src/parser/cxx/archive.h
@@ -70,7 +70,7 @@
 class ArchiveWriter {
  public:
   static constexpr std::string_view kMagic = "CXXARCH";
-  static constexpr std::uint32_t kSchemaMajor = 12;
+  static constexpr std::uint32_t kSchemaMajor = 13;
   static constexpr std::uint32_t kSchemaMinor = 0;

   void setEnvelope(ArchiveEnvelope envelope) {
